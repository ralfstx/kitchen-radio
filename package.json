{
  "name": "kitchen-radio",
  "version": "0.1.0",
  "dependencies": {
    "bluebird": "^3.3.3",
    "imagemagick": "^0.1.3",
    "mpd": "^1.3.0",
    "underscore": "^1.7.0",
    "winston": "^2.2.0"
  },
  "devDependencies": {
    "babel-cli": "^6.14.0",
    "babel-core": "^6.14.0",
    "babel-plugin-transform-es2015-modules-commonjs": "^6.14.0",
    "chai": "^3.5.0",
    "del": "^2.0.2",
    "eslint": "^3.4.0",
    "mocha": "^3.0.2",
    "node-fetch": "^1.6.0",
    "sinon": "^1.17.5"
  },
  "scripts": {
    "lint": "eslint .",
    "test": "mocha --compilers js:babel-core/register 'spec/**/*.spec.js'",
    "build": "rm -rf build/ && mkdir build/ && babel --compact false -d build/ src/ && cp -r src/web src/config.json package.json build/",
    "start": "node build/main.js",
    "deploy": "rsync -rv --delete --exclude 'config.json' --exclude 'node_modules/' build/ pi@radio:/var/lib/node/"
  }
}
